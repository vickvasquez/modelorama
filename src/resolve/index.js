const ControllersResolver = require('./controllers');
const ModelsResolver = require('./models');
const GraphQLResolver = require('./graphql');

module.exports = {
  ControllersResolver,
  ModelsResolver,
  GraphQLResolver,
};
